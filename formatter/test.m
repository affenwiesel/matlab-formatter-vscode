c = [5/3 5.5
c = [
(a <= 17 | a >
(a <= 17 | a >

for (index = 1:n)
    A = [-1, -2, -5E-25 i]; b = 3
    x = A * (-b');
    c = [5/3 5.5 344.33/45.66]
    x += 5; --x++

    if (a <= 17 | a > 25.55/36.53)
        printf('bla,blui %    blub + = 5', 55 + 5)
    end %endif

    x = 5 - a
    lsg_exact = @(y_0, t) t .+ ((1 / y_0)).^(-1);
    rechte_Seite = @(t, y) - y.^2;
end
